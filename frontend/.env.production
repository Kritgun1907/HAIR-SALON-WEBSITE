# VITE_BACKEND_URL is intentionally left EMPTY for production.
# The frontend uses relative /api/... paths, and Vercel rewrites (vercel.json)
# proxy those requests to api.thexpertshair.com server-side.
# This avoids cross-origin cookie issues (Safari ITP blocks cross-site cookies).
VITE_BACKEND_URL=
